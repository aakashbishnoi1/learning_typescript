// values are by default incremented from 0 if not defined
enum WeatherConditions {
    sunny = "sunny",
    cloudy = "cloudy",
    rainy = "rainy",
    snowy = "snowy",
}

let currentWeather = WeatherConditions.sunny;
console.log(currentWeather);
